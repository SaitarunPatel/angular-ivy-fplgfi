// import { createReducer, on } from '@ngrx/store';
// import { IncreWithNumber } from './counter.action';

// export const DataInitial: number = 19;

// export const _dataCounter = createReducer(
//   DataInitial,
//   on(IncreWithNumber, (state, { tarun }) => tarun + state)
// );

// export function DataCounter(state, action) {
//   return _dataCounter(state, action);
// }
